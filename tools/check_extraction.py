"""Tombstone — this file used to be a parallel parser fork.

Replaced by the wheelhouse approach (see .gitignore comment + D-009 in
docs/decisions.md). The real validation now runs ``pytest`` and ``obelisk``
CLI commands directly. Safe to ``git rm tools/check_extraction.py``.
"""
